import React from "react";
import { ContentRight } from "../styled-comp";

function RightView() {
   return (
      <ContentRight>
         <p>asdas</p>
      </ContentRight>
   );
}

export default React.memo(RightView);
