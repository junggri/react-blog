import React from "react";
import { ContentRight } from "../../styled-comp";

function RightView() {
   return (
      <ContentRight>

      </ContentRight>
   );
}

export default React.memo(RightView);
